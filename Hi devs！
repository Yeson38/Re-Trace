## 项目名称：通用代码执行可视化调试器（Unified Trace Debugger）

### 一、项目愿景
打造一款面向 OIer 和开发者的代码调试工具，核心能力为**“录制-回放”**：通过代码插桩，将任意主流语言的运行过程固化为标准时间线，在前端提供逐句步进、变量监控、循环计数和慢速播放体验。

### 二、核心架构决策（铁律）
- **统一范式**：**放弃解释器模拟与原生调试器适配**，全语言统一采用 **“源码插桩 + 录制 JSON + 前端回放”** 架构。
- **前后端分离**：后端（各语言适配器）仅负责生成 `trace.json`，前端播放器**与语言无关**，只消费 JSON。
- **关键取舍**：**不支持运行时修改变量后继续执行**（因是录制回放）。调试场景聚焦于“观察轨迹”与“定位首次异常”，此取舍可降低 80% 实现复杂度。

### 三、统一数据协议（前端播放器核心接口）
后端必须输出符合以下结构的 `trace.json`（数组形式）：

```typescript
interface TraceStep {
  line: number;           // 当前执行行号（从1开始）
  depth: number;          // 调用栈深度
  vars: { name: string; value: any; type: string }[]; // 当前作用域可见变量
  loops: { id: string; current: number; total: number }[]; // 侧边循环计数
  output: string;         // 本次步进追加的标准输出/打印
  globalStep: number;     // 全局累加步数（用于总计数器）
}
```
前端播放器实现：基于当前下标播放数组，提供**滑块拖拽回退**、**变量树展开**、**源码高亮联动**。

### 四、分阶段 Roadmap（总计约 14 周）

| 阶段 | 周期 | 任务 | 交付物 |
| :--- | :--- | :--- | :--- |
| **Phase 0** | 2周 | **前端播放器基建**：静态 HTML+TS，无框架依赖，实现三件套（高亮代码区、变量树、输出终端）及步进/滑块控件。手动造冒泡排序 `demo.json` 验证交互手感。 | 可独立运行的播放器页面 |
| **Phase 1** | 3周 | **Python 插桩适配器**：利用 `ast` 模块重写源码，在**每条语句前**注入 `emit_trace(locals())`，在循环节点注入计数器上报。运行插桩代码落盘 JSON。 | Python 版适配器脚本 + 配套使用文档 |
| **Phase 2** | 4周 | **C++ 插桩适配器**：基于正则匹配或 LibTooling（先上正则保证鲁棒性），在语句间隙插入 `__emit_trace` 宏，循环入口插入静态计数器。本地 g++ 编译运行出 JSON。 | C++ 版适配器（含宏定义头文件） |
| **Phase 3** | 3周 | **高级调试特性**：时间线二分搜索（定位变量首次等于某值）、循环复杂度自动估算（O(n)/O(n²)）、双时间点变量 Diff 对比。 | 功能更新包 |
| **Phase 4** | 2周 | **多端分发**：Web 端部署至 GitHub Pages（在线编辑 Python 可调 Pyodide）；桌面端用 **Tauri** 封装，调用本地 g++/python3 实现一键“插桩-编译-加载”。 | 网页链接 + 桌面安装包 |

### 五、技术栈建议
- **前端**：TypeScript + 原生 DOM（或轻量 Preact），CSS 变量主题，无后端依赖。
- **桌面壳**：Tauri（Rust 后端，体积小）。
- **Python 插桩**：标准库 `ast`、`inspect`。
- **C++ 插桩**：第一阶段用 `cppyy` 或正则实现快速原型；第二阶段若需高精度可迁移至 `Clang Tooling`。

### 六、开发注意事项
1. **变量序列化**：需处理循环引用（Python 对象/指针地址），建议用 `repr` 或限制递归深度转字符串。
2. **大文件性能**：录制 JSON 可能较大，前端播放器采用**分页懒加载**或 **Web Worker** 解析，避免 UI 卡顿。
3. **循环 ID 生成**：需根据 AST 节点唯一标识（如函数名+行号）生成 `loop_id`，确保嵌套循环不冲突。

---

开发者拿到此文档后可立即启动 **Phase 0**。如有疑问，优先确认 `TraceStep` 协议字段是否满足后端采集能力。
